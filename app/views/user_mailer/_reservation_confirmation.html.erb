<!Doctype html>
<html lang="en-US">

<head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
</head>

<body>
    <table width="100%" border="0" cellspacing="0" cellpadding="0">
        <tr>
            <td style="text-align: center; color: black;">
                <h1 class="confirmation-header">Tattoo appointment request confirmed!</h1>
                <% if (reservation.artwork.attachments.present?) %>
                    <% reservation.artwork.each do |artwork| %>
                        <div title="artwork" alt="artwork" style="height: 200px; width: 200px; background-image: linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0)), url('<%= rails_blob_url(artwork) %>'); background-position: center center; background-size: 100%;">
                        </div>
                        <% end %>
                            <% end  %>
                                <% if (reservation.body_area.attachments.present?) %>
                                    <% reservation.body_area.each do |image| %>
                                        <div style="height: 200px; width: 200px; background-image: linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0)), url('<%= rails_blob_url(image) %>'); background-position: center center; background-size: 100%;">
                                        </div>
                                        <% end %>
                                            <% end  %>
                                                <br>
                                                <p>
                                                    <%= reservation.artist.studio %>
                                                </p>
                                                <p>
                                                    <%= reservation.artist.name %>
                                                </p>
                                                <p>
                                                    <%= reservation.appt_date %>
                                                </p>
                                                <a href="https://www.madrabbit.com/" target="_blank" style="color: white; background: none; background-color: black; vertical-align: middle; padding: 0px 40px 0px 40px; text-decoration: none; border: none;">
                                                    CONNECT WITH ARTIST             
                                                </a>
            </td>
        </tr>
    </table>
</body>

</html>